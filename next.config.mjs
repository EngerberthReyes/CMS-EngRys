/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost'], // Puedes agregar dominios externos si los necesitas
      },
};

export default nextConfig;
